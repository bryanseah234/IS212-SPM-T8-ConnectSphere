# Backend

Reserved for the API, business logic, and database migrations. Language, framework,
database, and hosting are undecided. Python in `scripts/` is repository tooling,
not a backend selection.

When selecting the stack, add source, tests, dependencies/lockfile, a placeholder
`.env.example`, and relevant lint/type/build commands. Version migrations and
synthetic seed data. Define the API contract used by the frontend.
